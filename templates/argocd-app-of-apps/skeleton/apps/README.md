# Argo CD app-of-apps

This folder is the root `path` for the app-of-apps Argo CD `Application`.

Create subfolders under `apps/` (one per domain/project) and put child Argo CD `Application` manifests there.
