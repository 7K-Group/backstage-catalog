apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: ${{ values.name }}
  description: Backstage catalog entities for ${{ values.name }}
spec:
  type: url
  targets:
    - ./catalog/component.yaml
