apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: org-identities
  description: Collection of all Inari identity entities (organization, planes, teams, users)
spec:
  type: url
  targets:
    - ./acme/org-inari.yaml
